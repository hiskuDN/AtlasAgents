"""Setup script for AtlasAgents."""

from setuptools import setup, find_packages

# This is a minimal setup.py for compatibility
# The main configuration is in pyproject.toml

setup(
    packages=find_packages(),
)